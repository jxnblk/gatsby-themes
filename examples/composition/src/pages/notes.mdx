
# Notes

From typography.js docs:

```
base font / line height
scale ratio
rhythm unit
header/body font stack
Google fonts
header/body colors
font weights
```

```js
const styles = typeSystem({
  fontSize: '16px',
  lineHeight: 1.5,
  fontFamily: 'system-ui, sans-serif',
  ratio: 2,
  heading: {
    fontFamily: 'Georgia, serif',
    lineHeight: 1.25,
  },
  monospace: {
    fontFamily: 'Menlo, monospace',
  },
})
```
