
# Hello

This is `gatsby-theme-mono` *horizontally* composed with `gatsby-theme-notes`.
The Mono theme is built with [Theme UI][],
and can be used to style other Gatsby themes that use `gatsby-plugin-theme-ui`.

[theme ui]: https://theme-ui.com
