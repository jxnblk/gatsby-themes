import { Layout, Container } from 'gatsby-theme-system-2'

export default props =>
  <Layout>
    <Container>
      {props.children}
    </Container>
  </Layout>

# Hello, MDX!

This page is written in MDX and has a custom layout created with `gatsby-theme-system-2`
